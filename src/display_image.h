#ifndef _DISPLAY_IMAGE_H
#define _DISPLAY_IMAGE_H

#include <stdio.h>
#include "sh_image.h"

void display_image(FILE *handler, image_t *img, int true_color, int precision);

#endif /* _DISPLAY_IMAGE_H */
